"""业务服务层（Phase 1 起）。

为什么把服务层单独拎出来（而不是写在 `app/api/*.py` 里）：
API 层要处理的是 HTTP 语义（状态码、鉴权头、参数形状），服务层要处理的是业务规则
（状态机、幂等、重算口径）。混在一起会导致“想测业务规则必须先造一个 HTTP 请求”，
最终结果是没人写测试。现有 `api/chat.py` 里的逻辑因此也有下沉的空间（Phase 2 再做）。
"""
