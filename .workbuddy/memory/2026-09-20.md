# 2026-09-20

## DKD 项目 LangChain/LangGraph 智能体接入方案（V1.0，待评审）

- 完成 dkd-parent / dkd-app / dkd-vue 三子系统的架构分析：
  - dkd-parent = RuoYi 3.8.7（Spring Boot 2.5.15 + Java 8 + MyBatis + MySQL(dkd 库) + Redis，:8080），7 模块；dkd-app = 运维移动端 API（MyBatis-Plus，共库）；dkd-vue = Vue3 + Element Plus + Vite。
  - 业务域：Region/Partner/Node/VendingMachine/VmType/Channel/Sku/SkuClass/Policy/Order/Task(工单)/Inventory(库存+预警+补货建议 DTO)。
  - 已有初级 AI：`com.dkd.common.ai`（IAiService，Hutool HTTP 直连 DeepSeek 单轮对话，prompt 硬编码 Java switch）；前端 vm/node/inventory 页面有 AI 诊断、补货建议入口。
- 方案要点：新增 Python FastAPI + LangGraph 独立服务 dkd-agent(:8090)；Java 侧新增 agent 网关包（最小侵入）；读走 MySQL 只读账号、写走 Java REST 回调（复用 TaskServiceImpl 校验）；SSE 流式对话；LangGraph interrupt 实现 human-in-the-loop。
- 场景优先级：P0 智能补货 Agent + 设备故障诊断 Agent；P1 运营分析 Copilot（受控 NL2SQL）；P2 点位选址/货道配置。
- 分四阶段实施，约 10~12 周；首期写操作全部人工确认。
- 产出文档：`F:\DKD\docs\DKD智能体接入方案-LangChain-LangGraph.md`（含 6 个待评审决策点）。
- 发现的安全问题：application.yml 含明文 OSS AK/SK 与 DeepSeek API Key，方案中已列入 Phase 0 整改建议，尚未实际处理。

## 交互原型（V1 → V2）

- 原型 V1：`F:\DKD\docs\prototypes\dkd-agent-prototype.html`（单文件 HTML，模拟 RuoYi/Element Plus 风格）：补货工作台、设备诊断对话（工具调用chips+流式输出+病因置信度排序+维修工单卡片）、运营分析 Copilot、全局 AI 助手抽屉。数据均为模拟。
- 原型 V2 优化（用户反馈：信息过载、操作不清）：
  ①信息层级重构——补货理由折叠为"依据"展开行、表格列精简为6列、统计卡4→3、新增三步流程引导条；
  ②补货台人工干预闭环——调整弹窗（数量步进器+一键补满/减半/恢复建议值+必选调整原因+可折叠高级参数：预测窗口7/14/30天、服务水平slider联动缺货率）、跳过（必选原因）、恢复建议、整计划暂停/恢复（黄色横幅+确认弹窗）、每个操作均有确认弹窗+toast+状态标签反馈；
  ③Copilot 升级——4种图表（SVG手绘：趋势对比双折线/占比环形图/转化漏斗/排行条形），时间（今日/7天/30天）×维度（区域/点位/品类）筛选联动，自动洞察结论，SQL面板默认折叠；
  ④AI 助手快捷问题新增"暂停自动分析"指令。纯SVG无外部依赖。

## 排期计划

- `F:\DKD\docs\DKD智能体接入项目排期计划.md`：5 里程碑（M1 集成底座 W1-2 / M2 补货Agent W3-5 / M3 诊断Agent W6-7 / M4 Copilot+收尾 W8-10 / M5 12周观察期），基准日 2026-09-21，团队 1 Python + 1 Java(30%) + 前端(30%)；任务编号 Phase-序号可映射 Epic/Story；含风险缓冲（国庆假期、兼职投入、LLM质量兜底=统计基线不依赖LLM）与每周节奏建议。

## 开发规范 AGENTS.md

- `F:\DKD\AGENTS.md`（仓库根目录，供 AI 编码助手自动读取，参考用户提供的 Downloads\AGENTS.md 范本）：
  - Java 8/RuoYi 命名约定（I*Service/XxxController/selectXxxById、Dto/Vo 分包）、金额 BigDecimal、Controller 禁止业务逻辑；
  - 前端无 TS 勿引入、api/manage 分域、v-html 必须 sanitize；
  - 目录边界：业务代码只进 dkd-manage、target/ 禁读、com.dkd.common.ai 降级链路保留不删；
  - Git：main/develop/feature/fix 分支 + 中文 Conventional Commits（type(scope): 描述——补充说明）；
  - 测试增量标准（现状薄弱，修复必带回归用例、拒绝路径覆盖、Agent 测试 Fake LLM 不触网）；
  - 安全红线 9 条：明文密钥（待轮换）、MyBatis ${} 拼接、Agent 读写分离（写必走 Java 回调）、软删除、回调密钥与 JWT 分离、LLM 输出不可信（clamp/容错）、v-html sanitize、日志脱敏、HITL 写确认；
  - AI 协作流程：任务描述五要素模板（背景/范围/验收标准/约束/资料）、生成代码 7 项人工验收清单、AI 行为边界（只读不猜、不碰 target 与密钥、不隐瞒风险、单轮一事）、人工对 AI 代码署名负责。
